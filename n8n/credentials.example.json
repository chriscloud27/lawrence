{
  "_comment": "Placeholder documentation of the n8n credentials/env the committed workflows reference. NEVER commit real values. Create the OpenAI credential in the n8n UI; provide the env vars via n8n's environment (Docker env or n8n settings).",

  "credentials": [
    {
      "name": "OpenAI account",
      "type": "openAiApi",
      "usedBy": ["chat-agent → OpenAI Chat Model"],
      "fields": {
        "apiKey": "<your-openai-api-key>"
      }
    }
  ],

  "environmentVariables": {
    "_comment": "Referenced in workflow JSON as {{ $env.NAME }} — set these in n8n's environment, not in a credential.",
    "SUPABASE_URL": "https://<project-ref>.supabase.co",
    "SUPABASE_SERVICE_KEY": "<your-service-role-key>",
    "OPENAI_API_KEY": "<your-openai-api-key>",
    "CALENDLY_URL": "<your-calendly-link>"
  },

  "usedByWorkflow": {
    "chat-agent": {
      "OpenAI Chat Model": "openAiApi credential (attach in UI)",
      "search_schools": "SUPABASE_URL + SUPABASE_SERVICE_KEY (env)",
      "offer_calendar": "CALENDLY_URL (env)",
      "Score Lead": "OPENAI_API_KEY (env)"
    },
    "scrape-doris-school": {
      "OpenAI Extract": "OPENAI_API_KEY (env)",
      "Supabase REST nodes": "SUPABASE_URL + SUPABASE_SERVICE_KEY (env)"
    }
  }
}
