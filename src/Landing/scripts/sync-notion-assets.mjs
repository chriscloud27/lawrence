// Placeholder for the mach2.cloud-style Notion-sourced blog sync.
// Not wired into `npm run build` yet — run manually via `npm run sync:notion-assets`
// once NOTION_TOKEN / NOTION_DATABASE_ID are configured and the real sync logic lands.
// TODO: pull blog pages from Notion (@notionhq/client) and write local assets/content.

console.log("[sync:notion-assets] not implemented yet — skipping");
